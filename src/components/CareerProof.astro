---
import experience from '../data/experience.json';
---
<dl class="career-proof" aria-label="Career impact">
  {experience.metrics.map(metric => (
    <div><dt>{metric.label}</dt><dd>{metric.value}</dd><p>{metric.detail}</p></div>
  ))}
</dl>
